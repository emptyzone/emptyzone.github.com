<% if (tags.length > 0) {%>
    |
    <%
        var arr = [];
        tags.forEach(function(item){
                            arr.push('<a href="' + url_for(item.path) + '">' + item.name + '</a>');
        });
    %>
    <%- arr.join(', ') %>
<% } %>